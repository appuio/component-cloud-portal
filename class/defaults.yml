parameters:
  cloud_portal:
    =_metadata: {}
    namespace: appuio-cloud-portal

    release_name: cloud-portal
    charts:
      cloud-portal:
        source: https://charts.appuio.ch
        version: "0.4.1"

    helm_values: {}
