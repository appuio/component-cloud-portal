parameters:
  kapitan:
    compile:
      - input_paths:
          - cloud-portal/component/app.jsonnet
        input_type: jsonnet
        output_path: apps/
      - input_paths:
          - cloud-portal/component/main.jsonnet
        input_type: jsonnet
        output_path: cloud-portal/
